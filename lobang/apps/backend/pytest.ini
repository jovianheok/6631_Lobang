[pytest]
# Put the backend root on sys.path so `from src.… import …` works (the same
# absolute-import style main.py uses).
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
